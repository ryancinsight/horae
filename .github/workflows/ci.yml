name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: dtolnay/rust-toolchain@a5f673d0ba8626c3977bb416a1612774bc82181b
        with:
          components: clippy, rustfmt
      - uses: taiki-e/install-action@ea4c0b50b0d394ee91c5bb68a6a295b0556feb8e
      - uses: taiki-e/install-action@ea4c0b50b0d394ee91c5bb68a6a295b0556feb8e
        with:
          tool: mdbook
      - name: Format
        run: cargo fmt --all -- --check
      - name: Book tests
        run: |
          toolchain="$(rustup show active-toolchain | awk '{print $1}')"
          rustup run "$toolchain" cargo build --all-features --locked
          rustup run "$toolchain" rustdoc --version
          rustup run "$toolchain" mdbook test docs/book -L target/debug/deps
      - name: Feature check
        run: cargo check --no-default-features
      - name: Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: Native tests
        run: cargo nextest run --all-features
      - name: Documentation tests
        run: cargo test --doc --all-features
      - name: Documentation
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --no-deps --all-features
      - name: Example
        run: cargo run --example ordered_decay

  supply-chain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: dtolnay/rust-toolchain@a5f673d0ba8626c3977bb416a1612774bc82181b
      - uses: EmbarkStudios/cargo-deny-action@3c6349835b2b7b196a839186cb8b78e02f7b5f25
